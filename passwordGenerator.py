import sys

# Ask the user for minimum number of characters required from the user
max_chars = input("What is the maximum number of characters allowed in the password?: ")

# Ask the user for maximum number of characters required from the user
max_chars = input("What is the minumum number of characters allowed in the password?: ")

# Ask the user if an uppercase letter is required in the password
upper_required = input("Does the password require at least one uppercase letter (Y/N)? ")

# Ask the user if a lowercase letter is required in the password
lower_required = input("Does the password require at least one lowercase letter (Y/N)? ")
